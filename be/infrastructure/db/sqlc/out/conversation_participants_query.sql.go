// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: conversation_participants_query.sql

package out

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const addConversationParticipant = `-- name: AddConversationParticipant :one
INSERT INTO conversation_participants (
    conversation_id, user_id
) VALUES (
    $1, $2
)
RETURNING id, conversation_id, user_id, joined_at
`

type AddConversationParticipantParams struct {
	ConversationID pgtype.UUID
	UserID         pgtype.UUID
}

func (q *Queries) AddConversationParticipant(ctx context.Context, arg AddConversationParticipantParams) (ConversationParticipant, error) {
	row := q.db.QueryRow(ctx, addConversationParticipant, arg.ConversationID, arg.UserID)
	var i ConversationParticipant
	err := row.Scan(
		&i.ID,
		&i.ConversationID,
		&i.UserID,
		&i.JoinedAt,
	)
	return i, err
}

const addConversationParticipants = `-- name: AddConversationParticipants :many
INSERT INTO conversation_participants (
    conversation_id, user_id
) VALUES (
    $1, UNNEST($2::UUID[])
)
RETURNING id, conversation_id, user_id, joined_at
`

type AddConversationParticipantsParams struct {
	ConversationID pgtype.UUID
	Column2        []pgtype.UUID
}

func (q *Queries) AddConversationParticipants(ctx context.Context, arg AddConversationParticipantsParams) ([]ConversationParticipant, error) {
	rows, err := q.db.Query(ctx, addConversationParticipants, arg.ConversationID, arg.Column2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ConversationParticipant
	for rows.Next() {
		var i ConversationParticipant
		if err := rows.Scan(
			&i.ID,
			&i.ConversationID,
			&i.UserID,
			&i.JoinedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const isParticipantInConversation = `-- name: IsParticipantInConversation :one
SELECT EXISTS (
    SELECT 1
    FROM conversation_participants
    WHERE conversation_id = $1
      AND user_id = $2
) AS is_participant
`

type IsParticipantInConversationParams struct {
	ConversationID pgtype.UUID
	UserID         pgtype.UUID
}

func (q *Queries) IsParticipantInConversation(ctx context.Context, arg IsParticipantInConversationParams) (bool, error) {
	row := q.db.QueryRow(ctx, isParticipantInConversation, arg.ConversationID, arg.UserID)
	var is_participant bool
	err := row.Scan(&is_participant)
	return is_participant, err
}

const listParticipantsByConversation = `-- name: ListParticipantsByConversation :many
SELECT id, conversation_id, user_id, joined_at
FROM conversation_participants
WHERE conversation_id = $1
`

func (q *Queries) ListParticipantsByConversation(ctx context.Context, conversationID pgtype.UUID) ([]ConversationParticipant, error) {
	rows, err := q.db.Query(ctx, listParticipantsByConversation, conversationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ConversationParticipant
	for rows.Next() {
		var i ConversationParticipant
		if err := rows.Scan(
			&i.ID,
			&i.ConversationID,
			&i.UserID,
			&i.JoinedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removeConversationParticipant = `-- name: RemoveConversationParticipant :exec
DELETE FROM conversation_participants
WHERE conversation_id = $1
  AND user_id = $2
`

type RemoveConversationParticipantParams struct {
	ConversationID pgtype.UUID
	UserID         pgtype.UUID
}

func (q *Queries) RemoveConversationParticipant(ctx context.Context, arg RemoveConversationParticipantParams) error {
	_, err := q.db.Exec(ctx, removeConversationParticipant, arg.ConversationID, arg.UserID)
	return err
}
